[pytest]
# Run `pytest` from the project root and everything just works.
testpaths = tests
addopts = -q --strict-markers
filterwarnings =
    # Streamlit's AppTest harness runs the app outside a real script context and
    # is noisy about it. We care about our own warnings, not that one.
    ignore::DeprecationWarning:streamlit.*
